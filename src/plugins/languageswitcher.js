// src/plugins/languageswitcher.js
// Language switcher helper - disabled to let Docusaurus handle locale switching natively
// The localeDropdown in navbar works correctly in production builds

import ExecutionEnvironment from '@docusaurus/ExecutionEnvironment';

// This plugin is now disabled - Docusaurus handles locale switching automatically
// In development mode (npm start), you need to run separate servers for each locale
// In production/serve mode, all locales are available and switching works automatically
